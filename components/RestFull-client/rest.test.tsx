import RestFullAndGraph from './RestFullAndGraph';

describe('RestFullAndGraph Component', () => {
  it('renders RestFullAndGraph component correctly', async () => {
    expect(RestFullAndGraph).toBeDefined();
  });
});
